%#!platex ./report.tex


%
% 論文にのっている「解析解」のことはどうする？
%

%
% コンダクタンスの分布の様子は 平均値、分散の平均をとって、平均的な分布の様子を作ってみるか？ -> 平均値0のとき、分散の値は何でもよくなり,
% 平均的なガウス分布は考えにくい
%

\chapter{考察}
  \section{Passiveモデルについて, 先行研究との比較}
  結果の図\ref{Tsuishi_Rerative_volume}と図\ref{Tsuishi_Rerative_F}からわかるように
  本研究手法と先行研究手法を比較すると, $F$値は${\Delta}t = 5$[ms]
  以外では有意差がみられかったが, 体積は全ての${\Delta}t$で大幅に減少した.
  このことから本研究手法は神経細胞の形態の最適化において, 先行研究手法よりも効果的であると
  考えられる. 
  \section{Kaモデルについて}
    \subsection{${\Delta}t = 5$[ms]の結果について}
    先行研究ではKaチャネルを導入した場合は$F$の値は${\Delta}t$と反比例する
    という結果になっていた. 本研究の結果ではこれを再現することはできなかった. これには
    以下の原因が考えられる.
    \begin{enumerate}
      \item 評価式の$Morpho\hspace{1mm}ratio$が大きすぎるため形態が小さくなり, 
        細胞の表面積が小さくなったため.
      \item Kaコンダクタンスの最大値(0.22[S/c${\rm m^2}$])が小さすぎるため
    \end{enumerate}
    つまり神経細胞上に分布するKaコンダクタンスの絶対的な量が少なかったせいではないかと考えられる. 
    \subsection{コンダクタンス分布様式による違いについて}
    図\ref{k_F}より2つの分布様式のどちらであっても$F$値に有意差は見られなかった. 
    また図\ref{k_Ka_dist_dt10}に示されるように, コンダクタンス分布様式に線形分布,
    ガウス分布のどちらを用いてもKaコンダクタンスはUpper Dendriteの上に多く分布
    していることがわかる. また, 線形分布の場合をみるとUpper DendriteのKaコンダクタンスは
    細胞体の付近で急激に少なくなっている. 
    ガウス分布の場合ではそのような傾向はみられなかった. これは, 樹状突起の先端は一様に高い
    コンダクタンス, 根元付近では低いコンダクタンスという分布を, 対称な分布であるガウス分布
    で表現するのは困難であったためと考えられる.
    %
    % Stem付近のコンダクタンスはUpper とLowerで近い値になっている
    %
    \subsection{コンダクタンス量評価の影響}
    図\ref{k_TREE_K_ratio}を見ると線形分布では${\Delta}t = 15$[ms]のとき
    個体評価にコンダクタンス量の考慮を入れる場合と入れない場合でコンダクタンスの含有量に有意差が
    見られた. 一方で図\ref{k_F}より$F$値には有意な差は見られない. 図\ref{k_Ka_dist_dt15}と
    図\ref{k_upper_k_ratio}を見ると線形分布ではUpper Dendriteに有意なコンダクタンス含有率の
    変化が見られた. 
    
    %結果としてガウス分布で線形分布の形質を表現することができていなかった
    %もしくはコンダクタンス分布の変化の単位が影響しているかも
  \section{CaTモデルについて}
    \subsection{コンダクタンス分布様式による違いについて}
    図\ref{ca_F}を見ると, 2つのコンダクタンス分布様式の結果の間に有意な差は見られなかった.
    一方で\ref{ca_TREE_Ca_ratio}

    \subsection{コンダクタンス量評価の影響}
    図\ref{ca_TREE_Ca_ratio}より, コンダクタンス分布様式にガウス分布を用いている状態で
    個体評価にコンダクタンス量への考慮を導入すると, すべての${\Delta}t$でコンダクタンス含有率に
    有意な差が見られた.  また図\ref{ca_Ca_dist}ではコンダクタンスへの制限を導入した場合,
    樹状突起の先端にCaTコンダクタンスが集中して分布する傾向が見られた.
    % ガウス分布の平均値を見ればどこにピークがあるかはわかるはず
    \section{Ka, CaTモデルについて}
    

